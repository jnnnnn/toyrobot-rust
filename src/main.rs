mod model;
mod input;

// prepare the list of commands (PLACE, MOVE, and TURN) and then call them as lines of text input dictate.
fn main() {
    let mut table = model::Table::new(5, 5);
    let mut robot:Option<model::Robot> = None;

    loop {

    }
}
